pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'hello world'
                echo 'Triggered webhook'
                echo "added one more line"
            }
        }
    }
}
